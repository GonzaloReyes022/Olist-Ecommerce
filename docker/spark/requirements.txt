# ==========================================
# CORE - PySpark viene con la imagen
# ==========================================
# delta-spark se instala por separado en Dockerfile con --no-deps
# para evitar que pip reinstale pyspark (ya viene bundled en la imagen)

# ==========================================
# SNOWFLAKE
# ==========================================
snowflake-connector-python==3.12.0
# Conector Python a Snowflake (sin Spark) - 3.12.x no pina cryptography <42

# ==========================================
# DATA PROCESSING
# ==========================================
pyarrow==14.0.2
# Formato columnar eficiente para Spark

# ==========================================
# DATA QUALITY (alternativas ligeras)
# ==========================================
# great-expectations==0.18.8  # Comentado: muy pesado
pandera==0.20.4
# Alternativa ligera para validación de esquemas (0.20.x tiene soporte estable para pandas 2.x)

# ==========================================
# UTILITIES
# ==========================================
python-dotenv==1.0.0
requests==2.31.0

# ==========================================
# ML
# ==========================================
xgboost==2.0.3
# Gradient Boosting: modelo principal para predicción LTV
scikit-learn==1.3.2
# Preprocessing (LabelEncoder), métricas (classification_report)
# Nota: 1.4.0+ requiere Python 3.9+; la imagen apache/spark:3.5.1-python3 usa Python 3.8
pandas==2.0.3
# Conversión Spark → pandas para entrenamiento XGBoost
# Nota: 2.1.x requiere Python 3.9+; la imagen apache/spark:3.5.1-python3 usa Python 3.8

# ==========================================
# TESTING
# ==========================================
pytest==7.4.3
pytest-spark==0.6.0
chispa==0.9.4
# Testing específico para DataFrames Spark